#!/bin/bash
# Launch Portainer container using docker-compose
# Generated by Claude

cd "$(dirname "$0")"
docker-compose up -d
echo "Portainer started. Open https://localhost:9443"
echo "Login: admin / Portainer123!"
