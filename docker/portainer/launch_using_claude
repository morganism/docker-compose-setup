#!/bin/bash
# Launch Portainer using Claude Code
# This script invokes Claude to checkout the repo, launch portainer, and open the browser

claude --print "checkout this github repo 'morganism/docker-compose-setup' and then launch the portainer docker compose container (in sub directory: docker/portainer) then open 'https://localhost:9443' user=admin password=Portainer123!"
